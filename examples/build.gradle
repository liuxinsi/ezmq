apply plugin: 'java'

sourceCompatibility = 1.8
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.slf4j:slf4j-simple:1.7.12'
    compile fileTree(dir: project.parent.buildDir.absolutePath + '/libs', include: ['*.jar'])
}
